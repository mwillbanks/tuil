import type { RegistryItem } from "@mwillbanks/tuil-registry";

// Generated by tooling/registry/build.ts from the source-owned registry.
export const generatedRegistryItems = [
  {
    name: "alert",
    type: "feedback",
    tier: 3,
    title: "Alert",
    description: "Semantic terminal alert.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    semantics: ["alert"],
    dependencies: ["@mwillbanks/tuil-theme", "react"],
    slots: ["root", "title", "content", "icon"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "replace",
    },
    files: [
      {
        path: "../../../registry/feedback/alert.tsx",
        target: "src/components/tuil/feedback/alert.tsx",
        content:
          'import {\n  resolveSlotProps,\n  type SlottedComponentProps,\n  useTheme,\n} from "@mwillbanks/tuil-theme";\nimport type { ReactNode } from "react";\nimport { Text, type TextProps } from "../data-display/text";\nimport { Box, type BoxProps } from "../primitives/box";\n\ntype Tone = "info" | "success" | "warning" | "danger";\n\nexport interface AlertProps\n  extends SlottedComponentProps<\n    {\n      root: BoxProps;\n      icon: TextProps;\n      title: TextProps;\n      content: TextProps;\n    },\n    { readonly tone: Tone }\n  > {\n  readonly id?: string;\n  readonly testId?: string;\n  readonly label?: string;\n  readonly description?: string;\n  readonly disabled?: boolean;\n  readonly readOnly?: boolean;\n  readonly tone?: Tone;\n  readonly title?: string;\n  readonly children?: ReactNode;\n}\n\nexport function Alert({\n  tone = "info",\n  title,\n  children,\n  slots,\n  slotProps,\n  unstyled,\n  ...props\n}: AlertProps): ReactNode {\n  const theme = useTheme();\n  const state = { tone };\n  const Root = slots?.root ?? Box;\n  const Icon = slots?.icon ?? Text;\n  const Title = slots?.title ?? Text;\n  const Content = slots?.content ?? Text;\n  const icon =\n    tone === "success"\n      ? theme.icons.success\n      : tone === "warning"\n        ? theme.icons.warning\n        : tone === "danger"\n          ? theme.icons.error\n          : "i";\n  return (\n    <Root\n      {...props}\n      {...resolveSlotProps(slotProps?.root, state, theme)}\n      borderStyle={unstyled ? undefined : "round"}\n      borderColor={unstyled ? undefined : theme.colors[tone].border}\n      padding={unstyled ? undefined : "sm"}\n      role="alert"\n      label={props.label ?? title}\n    >\n      <Icon\n        {...resolveSlotProps(slotProps?.icon, state, theme)}\n        color={theme.colors[tone].foreground}\n      >\n        {icon}\n      </Icon>\n      {title ? (\n        <Title\n          {...resolveSlotProps(slotProps?.title, state, theme)}\n          bold\n          color={theme.colors[tone].foreground}\n        >\n          {title}\n        </Title>\n      ) : null}\n      {children ? (\n        <Content {...resolveSlotProps(slotProps?.content, state, theme)}>\n          {children}\n        </Content>\n      ) : null}\n    </Root>\n  );\n}\n',
      },
    ],
    registryDependencies: ["box", "text"],
  },
  {
    name: "app-bar",
    type: "component",
    tier: 3,
    title: "App Bar",
    description: "Application header region.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    dependencies: ["react"],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "replace",
    },
    files: [
      {
        path: "../../../registry/components/app-bar.tsx",
        target: "src/components/tuil/components/app-bar.tsx",
        content:
          'import type { ReactNode } from "react";\nimport { Box, type BoxProps } from "../primitives/box";\n\nexport function AppBar(props: BoxProps): ReactNode {\n  return <Box flexDirection="row" {...props} />;\n}\n',
      },
    ],
    registryDependencies: ["box"],
  },
  {
    name: "app-shell",
    type: "component",
    tier: 3,
    title: "App Shell",
    description: "Compound application shell.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    semantics: ["application"],
    dependencies: ["react"],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "replace",
    },
    files: [
      {
        path: "../../../registry/components/app-shell.tsx",
        target: "src/components/tuil/components/app-shell.tsx",
        content:
          'import type { ReactNode } from "react";\nimport { Box, type BoxProps } from "../primitives/box";\nimport { AppBar } from "./app-bar";\nimport { StatusBar } from "./status-bar";\n\nfunction Main(props: BoxProps): ReactNode {\n  return <Box flexGrow={1} {...props} />;\n}\n\nexport function AppShell(props: BoxProps): ReactNode {\n  return <Box flexDirection="column" role="application" {...props} />;\n}\n\nAppShell.AppBar = AppBar;\nAppShell.Main = Main;\nAppShell.StatusBar = StatusBar;\n',
      },
    ],
    registryDependencies: ["box", "app-bar", "status-bar"],
  },
  {
    name: "badge",
    type: "data-display",
    tier: 3,
    title: "Badge",
    description: "Semantic status badge.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    dependencies: ["@mwillbanks/tuil-theme", "react"],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "replace",
    },
    files: [
      {
        path: "../../../registry/data-display/badge.tsx",
        target: "src/components/tuil/data-display/badge.tsx",
        content:
          'import { useTheme } from "@mwillbanks/tuil-theme";\nimport type { ReactNode } from "react";\nimport { Text, type TextProps } from "./text";\n\nexport interface BadgeProps extends TextProps {\n  readonly tone?: "neutral" | "success" | "warning" | "danger" | "info";\n}\n\nexport function Badge({\n  tone = "neutral",\n  children,\n  ...props\n}: BadgeProps): ReactNode {\n  const theme = useTheme();\n  const color =\n    tone === "neutral" ? theme.colors.muted : theme.colors[tone].foreground;\n  return (\n    <Text color={color} {...props}>\n      [{children}]\n    </Text>\n  );\n}\n',
      },
    ],
    registryDependencies: ["text"],
  },
  {
    name: "box",
    type: "primitive",
    tier: 1,
    title: "Box",
    description: "Theme-aware Ink layout primitive.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    semantics: ["application"],
    dependencies: [
      "@mwillbanks/tuil-ink",
      "@mwillbanks/tuil-theme",
      "ink",
      "react",
    ],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "adapt",
    },
    files: [
      {
        path: "../../../registry/primitives/box.tsx",
        target: "src/components/tuil/primitives/box.tsx",
        content:
          'import {\n  type BoxProps as TuilBoxProps,\n  useSemanticNode,\n} from "@mwillbanks/tuil-ink";\nimport {\n  resolveComponentProps,\n  resolveSlotProps,\n  type SpacingToken,\n  useTheme,\n} from "@mwillbanks/tuil-theme";\nimport { Box as InkBox } from "ink";\nimport { type ReactNode, useId, useMemo } from "react";\n\nexport type BoxProps = TuilBoxProps;\n\nexport function Box({\n  slots,\n  slotProps,\n  padding,\n  margin,\n  children,\n  id,\n  testId,\n  role,\n  label,\n  description,\n  disabled,\n  readOnly,\n  selected,\n  checked,\n  expanded,\n  valueText,\n  unstyled,\n  variant,\n  size,\n  className,\n  ...props\n}: BoxProps): ReactNode {\n  const theme = useTheme();\n  const generated = useId();\n  const key = id ?? generated;\n  const semantics = useMemo(\n    () => ({\n      key,\n      id: id ?? key,\n      testId,\n      role,\n      label,\n      description,\n      disabled,\n      readOnly,\n      selected,\n      checked,\n      expanded,\n      valueText,\n    }),\n    [\n      checked,\n      description,\n      disabled,\n      expanded,\n      id,\n      key,\n      label,\n      readOnly,\n      role,\n      selected,\n      testId,\n      valueText,\n    ],\n  );\n  useSemanticNode(semantics);\n  const Root = slots?.root ?? InkBox;\n  const customization = resolveComponentProps(\n    "Box",\n    { variant, size, unstyled, className },\n    {},\n    theme,\n  );\n  const resolveSpacing = (value: number | SpacingToken | undefined) =>\n    typeof value === "string" ? theme.spacing[value] : value;\n  return (\n    <Root\n      {...(customization as TuilBoxProps)}\n      {...props}\n      {...resolveSlotProps(slotProps?.root, {}, theme)}\n      padding={resolveSpacing(padding)}\n      margin={resolveSpacing(margin)}\n    >\n      {children}\n    </Root>\n  );\n}\n',
      },
    ],
    registryDependencies: [],
  },
  {
    name: "button",
    type: "component",
    tier: 3,
    title: "Button",
    description: "Focusable command-aware button.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    semantics: ["button"],
    dependencies: [
      "@mwillbanks/tuil",
      "@mwillbanks/tuil-focus",
      "@mwillbanks/tuil-hotkeys",
      "@mwillbanks/tuil-ink",
      "@mwillbanks/tuil-theme",
      "ink",
      "react",
    ],
    slots: ["root", "label", "prefix", "suffix"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "replace",
    },
    files: [
      {
        path: "../../../registry/components/button.tsx",
        target: "src/components/tuil/components/button.tsx",
        content:
          'import { useApp } from "@mwillbanks/tuil";\nimport { useFocusable } from "@mwillbanks/tuil-focus";\nimport { useHotkey, useHotkeys } from "@mwillbanks/tuil-hotkeys";\nimport {\n  type ButtonProps as TuilButtonProps,\n  useSemanticNode,\n} from "@mwillbanks/tuil-ink";\nimport {\n  resolveComponentProps,\n  resolveSlotProps,\n  useTheme,\n} from "@mwillbanks/tuil-theme";\nimport {\n  Box as InkBox,\n  type BoxProps as InkBoxProps,\n  Text as InkText,\n} from "ink";\nimport { type ReactNode, useEffect, useId, useMemo } from "react";\n\nexport type ButtonProps = TuilButtonProps;\n\nexport function Button({\n  children,\n  onPress,\n  command,\n  hotkeys = [],\n  autoFocus,\n  focusOrder,\n  focusScopeId,\n  slots,\n  slotProps,\n  disabled = false,\n  readOnly = false,\n  prefix,\n  suffix,\n  variant = "solid",\n  size,\n  unstyled,\n  className,\n  ...props\n}: ButtonProps): ReactNode {\n  const app = useApp();\n  const theme = useTheme();\n  const generated = useId();\n  const id = props.id ?? generated;\n  const label = props.label ?? (typeof children === "string" ? children : id);\n  const focusable = useMemo(\n    () => ({\n      id,\n      scopeId: focusScopeId,\n      disabled,\n      hidden: false,\n      order: focusOrder,\n      role: "button",\n      label,\n    }),\n    [disabled, focusOrder, focusScopeId, id, label],\n  );\n  const { focused, focus } = useFocusable(focusable);\n  useEffect(() => {\n    if (autoFocus) focus();\n  }, [autoFocus, focus]);\n  const activate = async () => {\n    if (disabled || readOnly) return;\n    if (command) {\n      await app.commands.execute(command, { source: id });\n    }\n    await onPress?.();\n  };\n  useHotkey("enter", activate, {\n    scope: "application",\n    priority: 100,\n    enabled: () => app.focus.focusedId === id && !disabled && !readOnly,\n    title: label,\n    commandId: command,\n  });\n  useHotkey("space", activate, {\n    scope: "application",\n    priority: 100,\n    enabled: () => app.focus.focusedId === id && !disabled && !readOnly,\n    title: label,\n    commandId: command,\n  });\n  useHotkeys(Object.fromEntries(hotkeys.map((keys) => [keys, activate])), {\n    scope: "application",\n    enabled: () => !disabled && !readOnly,\n    title: label,\n    commandId: command,\n  });\n  const semanticNode = useMemo(\n    () => ({\n      key: id,\n      id,\n      testId: props.testId,\n      role: "button" as const,\n      label,\n      description: props.description,\n      disabled,\n      readOnly,\n    }),\n    [disabled, id, label, props.description, props.testId, readOnly],\n  );\n  useSemanticNode(semanticNode);\n  const state = { focused, disabled };\n  const Root = slots?.root ?? InkBox;\n  const Label = slots?.label ?? InkText;\n  const Prefix = slots?.prefix ?? InkText;\n  const Suffix = slots?.suffix ?? InkText;\n  const color =\n    variant === "danger"\n      ? theme.colors.danger.foreground\n      : theme.colors.primary.foreground;\n  const marker =\n    app.mode === "interactive"\n      ? focused\n        ? app.capabilities.unicode\n          ? "▶"\n          : ">"\n        : " "\n      : "-";\n  const customization = resolveComponentProps(\n    "Button",\n    { variant, size, unstyled, className },\n    state,\n    theme,\n  );\n  return (\n    <Root\n      {...(customization as InkBoxProps)}\n      {...resolveSlotProps(slotProps?.root, state, theme)}\n    >\n      <Prefix {...resolveSlotProps(slotProps?.prefix, state, theme)}>\n        {prefix ?? marker}\n      </Prefix>\n      <Label\n        color={disabled ? theme.colors.muted : color}\n        bold={focused}\n        dimColor={disabled}\n        {...resolveSlotProps(slotProps?.label, state, theme)}\n      >\n        {app.mode === "interactive"\n          ? `[ ${children} ]`\n          : String(children ?? label)}\n      </Label>\n      {suffix ? (\n        <Suffix {...resolveSlotProps(slotProps?.suffix, state, theme)}>\n          {suffix}\n        </Suffix>\n      ) : null}\n    </Root>\n  );\n}\n',
      },
    ],
    registryDependencies: [],
  },
  {
    name: "container",
    type: "primitive",
    tier: 1,
    title: "Container",
    description: "Width-constrained terminal container.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    dependencies: ["react"],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "adapt",
    },
    files: [
      {
        path: "../../../registry/primitives/container.tsx",
        target: "src/components/tuil/primitives/container.tsx",
        content:
          'import type { ReactNode } from "react";\nimport { Box, type BoxProps } from "./box";\n\nexport interface ContainerProps extends BoxProps {\n  readonly maxWidth?: number;\n}\n\nexport function Container({\n  maxWidth = 80,\n  width = "100%",\n  ...props\n}: ContainerProps): ReactNode {\n  return <Box width={width} maxWidth={maxWidth} {...props} />;\n}\n',
      },
    ],
    registryDependencies: ["box"],
  },
  {
    name: "default",
    type: "theme",
    tier: 3,
    title: "Default Theme",
    description: "Default tuil theme.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    dependencies: ["@mwillbanks/tuil"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "replace",
    },
    files: [
      {
        path: "../../../registry/themes/default.ts",
        target: "src/lib/theme.ts",
        content:
          'import { createTheme } from "@mwillbanks/tuil";\n\nexport const theme = createTheme({\n  id: "tuil-default",\n  colorScheme: "dark",\n});\n',
      },
    ],
    registryDependencies: [],
  },
  {
    name: "divider",
    type: "data-display",
    tier: 3,
    title: "Divider",
    description: "Terminal-safe divider.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    dependencies: ["@mwillbanks/tuil-theme", "react"],
    registryDependencies: ["text"],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "adapt",
    },
    files: [
      {
        path: "../../../registry/data-display/divider.tsx",
        target: "src/components/tuil/data-display/divider.tsx",
        content:
          'import {\n  resolveSlotProps,\n  type SlottedComponentProps,\n  useTheme,\n} from "@mwillbanks/tuil-theme";\nimport type { ReactNode } from "react";\nimport { Text, type TextProps } from "./text";\n\nexport interface DividerProps\n  extends SlottedComponentProps<{ root: TextProps }> {\n  readonly id?: string;\n  readonly testId?: string;\n  readonly label?: string;\n  readonly width?: number;\n  readonly orientation?: "horizontal" | "vertical";\n  readonly title?: string;\n}\n\nexport function Divider({\n  width = 40,\n  orientation = "horizontal",\n  title,\n  slots,\n  slotProps,\n  ...props\n}: DividerProps): ReactNode {\n  const theme = useTheme();\n  const Root = slots?.root ?? Text;\n  const line = theme.borders.single[0];\n  const content =\n    orientation === "vertical"\n      ? theme.borders.single[1]\n      : title\n        ? `${line.repeat(2)} ${title} ${line.repeat(Math.max(0, width - title.length - 4))}`\n        : line.repeat(width);\n  return (\n    <Root\n      {...props}\n      {...resolveSlotProps(slotProps?.root, {}, theme)}\n      label={props.label ?? title ?? "separator"}\n    >\n      {content}\n    </Root>\n  );\n}\n',
      },
    ],
  },
  {
    name: "heading",
    type: "data-display",
    tier: 3,
    title: "Heading",
    description: "Semantic terminal heading.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    semantics: ["heading"],
    dependencies: ["@mwillbanks/tuil-theme", "react"],
    registryDependencies: ["text"],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "adapt",
    },
    files: [
      {
        path: "../../../registry/data-display/heading.tsx",
        target: "src/components/tuil/data-display/heading.tsx",
        content:
          'import { useTheme } from "@mwillbanks/tuil-theme";\nimport type { ReactNode } from "react";\nimport { Text, type TextProps } from "./text";\n\nexport interface HeadingProps extends TextProps {\n  readonly level?: 1 | 2 | 3 | 4;\n}\n\nexport function Heading({\n  level = 1,\n  children,\n  ...props\n}: HeadingProps): ReactNode {\n  const theme = useTheme();\n  return (\n    <Text\n      bold={theme.typography.headingBold}\n      role="heading"\n      label={typeof children === "string" ? children : props.label}\n      {...props}\n    >\n      {"#".repeat(level)} {children}\n    </Text>\n  );\n}\n',
      },
    ],
  },
  {
    name: "progress",
    type: "feedback",
    tier: 3,
    title: "Progress",
    description: "Bounded terminal progress bar.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    semantics: ["progressbar"],
    dependencies: ["@mwillbanks/tuil", "@mwillbanks/tuil-theme", "react"],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "replace",
    },
    files: [
      {
        path: "../../../registry/feedback/progress.tsx",
        target: "src/components/tuil/feedback/progress.tsx",
        content:
          'import { useApp } from "@mwillbanks/tuil";\nimport {\n  resolveSlotProps,\n  type SlottedComponentProps,\n  useTheme,\n} from "@mwillbanks/tuil-theme";\nimport type { ReactNode } from "react";\nimport { Text, type TextProps } from "../data-display/text";\n\ninterface ProgressState {\n  readonly percent: number;\n  readonly complete: boolean;\n}\n\nexport interface ProgressProps\n  extends SlottedComponentProps<{ root: TextProps }, ProgressState> {\n  readonly id?: string;\n  readonly testId?: string;\n  readonly label?: string;\n  readonly value: number;\n  readonly max?: number;\n  readonly width?: number;\n  readonly showValue?: boolean;\n  readonly disabled?: boolean;\n  readonly readOnly?: boolean;\n}\n\nexport function Progress({\n  value,\n  max = 100,\n  width = 20,\n  showValue = true,\n  slots,\n  slotProps,\n  ...props\n}: ProgressProps): ReactNode {\n  const app = useApp();\n  const theme = useTheme();\n  const ratio = max <= 0 ? 0 : Math.max(0, Math.min(1, value / max));\n  const completeCells = Math.round(width * ratio);\n  const percent = Math.round(ratio * 100);\n  const completeGlyph = app.capabilities.unicode ? "█" : "#";\n  const remainingGlyph = app.capabilities.unicode ? "░" : "-";\n  const Root = slots?.root ?? Text;\n  return (\n    <Root\n      {...props}\n      {...resolveSlotProps(\n        slotProps?.root,\n        { percent, complete: ratio >= 1 },\n        theme,\n      )}\n      role="progressbar"\n      label={props.label ?? `${percent}%`}\n      valueText={`${percent}%`}\n      color={theme.colors.primary.foreground}\n    >\n      [\n      {`${completeGlyph.repeat(completeCells)}${remainingGlyph.repeat(width - completeCells)}`}\n      ]{showValue ? ` ${percent}%` : ""}\n    </Root>\n  );\n}\n',
      },
    ],
    registryDependencies: ["text"],
  },
  {
    name: "spinner",
    type: "feedback",
    tier: 3,
    title: "Spinner",
    description: "Capability-aware activity spinner.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    semantics: ["status"],
    dependencies: ["@mwillbanks/tuil", "@mwillbanks/tuil-theme", "react"],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "replace",
    },
    files: [
      {
        path: "../../../registry/feedback/spinner.tsx",
        target: "src/components/tuil/feedback/spinner.tsx",
        content:
          'import { useApp } from "@mwillbanks/tuil";\nimport {\n  resolveSlotProps,\n  type SlottedComponentProps,\n  useTheme,\n} from "@mwillbanks/tuil-theme";\nimport { type ReactNode, useEffect, useState } from "react";\nimport { Text, type TextProps } from "../data-display/text";\n\nexport interface SpinnerProps\n  extends SlottedComponentProps<{ root: TextProps }> {\n  readonly id?: string;\n  readonly testId?: string;\n  readonly label?: string;\n  readonly disabled?: boolean;\n  readonly readOnly?: boolean;\n}\n\nexport function Spinner({\n  label = "Loading",\n  slots,\n  slotProps,\n  ...props\n}: SpinnerProps): ReactNode {\n  const app = useApp();\n  const theme = useTheme();\n  const [frame, setFrame] = useState(0);\n  useEffect(() => {\n    if (app.mode !== "interactive" || !theme.motion.enabled) return;\n    const timer = setInterval(\n      () =>\n        setFrame((value) => (value + 1) % theme.motion.spinnerFrames.length),\n      theme.motion.interval,\n    );\n    return () => clearInterval(timer);\n  }, [app.mode, theme]);\n  const Root = slots?.root ?? Text;\n  const symbol =\n    app.mode === "interactive"\n      ? theme.motion.spinnerFrames[frame]\n      : theme.icons.pending;\n  return (\n    <Root\n      {...props}\n      {...resolveSlotProps(slotProps?.root, {}, theme)}\n      role="status"\n      label={label}\n    >\n      {symbol} {label}\n    </Root>\n  );\n}\n',
      },
    ],
    registryDependencies: ["text"],
  },
  {
    name: "stack",
    type: "primitive",
    tier: 1,
    title: "Stack",
    description: "Vertical, horizontal, and configurable stack layouts.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    dependencies: ["@mwillbanks/tuil-theme", "react"],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "adapt",
    },
    files: [
      {
        path: "../../../registry/primitives/stack.tsx",
        target: "src/components/tuil/primitives/stack.tsx",
        content:
          'import { type SpacingToken, useTheme } from "@mwillbanks/tuil-theme";\nimport type { ReactNode } from "react";\nimport { Box, type BoxProps } from "./box";\n\nexport interface StackProps extends Omit<BoxProps, "gap"> {\n  readonly direction?: "row" | "column";\n  readonly gap?: number | SpacingToken;\n}\n\nexport function Stack({\n  direction = "column",\n  gap,\n  ...props\n}: StackProps): ReactNode {\n  const theme = useTheme();\n  return (\n    <Box\n      flexDirection={direction}\n      gap={typeof gap === "string" ? theme.spacing[gap] : gap}\n      {...props}\n    />\n  );\n}\n\nexport function HStack(props: Omit<StackProps, "direction">): ReactNode {\n  return <Stack direction="row" {...props} />;\n}\n\nexport function VStack(props: Omit<StackProps, "direction">): ReactNode {\n  return <Stack direction="column" {...props} />;\n}\n',
      },
    ],
    registryDependencies: ["box"],
  },
  {
    name: "status-bar",
    type: "component",
    tier: 3,
    title: "Status Bar",
    description: "Persistent status region.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    semantics: ["status"],
    dependencies: ["react"],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "replace",
    },
    files: [
      {
        path: "../../../registry/components/status-bar.tsx",
        target: "src/components/tuil/components/status-bar.tsx",
        content:
          'import type { ReactNode } from "react";\nimport { Box, type BoxProps } from "../primitives/box";\n\nexport function StatusBar(props: BoxProps): ReactNode {\n  return <Box flexDirection="row" role="status" {...props} />;\n}\n',
      },
    ],
    registryDependencies: ["box"],
  },
  {
    name: "text",
    type: "data-display",
    tier: 1,
    title: "Text",
    description: "Semantic terminal text.",
    renderer: "ink",
    capabilities: ["interactive", "static"],
    semantics: ["text"],
    dependencies: [
      "@mwillbanks/tuil-ink",
      "@mwillbanks/tuil-theme",
      "ink",
      "react",
    ],
    slots: ["root"],
    provenance: {
      source: "tuil",
      license: "MIT",
      mode: "adapt",
    },
    files: [
      {
        path: "../../../registry/data-display/text.tsx",
        target: "src/components/tuil/data-display/text.tsx",
        content:
          'import {\n  type TextProps as TuilTextProps,\n  useSemanticNode,\n} from "@mwillbanks/tuil-ink";\nimport {\n  resolveComponentProps,\n  resolveSlotProps,\n  useTheme,\n} from "@mwillbanks/tuil-theme";\nimport { Text as InkText } from "ink";\nimport { type ReactNode, useId, useMemo } from "react";\n\nexport type TextProps = TuilTextProps;\n\nexport function Text({\n  slots,\n  slotProps,\n  children,\n  id,\n  testId,\n  role,\n  label,\n  description,\n  disabled,\n  readOnly,\n  selected,\n  checked,\n  expanded,\n  valueText,\n  unstyled,\n  variant,\n  size,\n  className,\n  ...props\n}: TextProps): ReactNode {\n  const theme = useTheme();\n  const generated = useId();\n  const key = id ?? generated;\n  const text =\n    typeof children === "string" || typeof children === "number"\n      ? String(children)\n      : undefined;\n  const semantics = useMemo(\n    () => ({\n      key,\n      id: id ?? key,\n      testId,\n      role: role ?? "text",\n      label: label ?? text,\n      text,\n      description,\n      disabled,\n      readOnly,\n      selected,\n      checked,\n      expanded,\n      valueText,\n    }),\n    [\n      checked,\n      description,\n      disabled,\n      expanded,\n      id,\n      key,\n      label,\n      readOnly,\n      role,\n      selected,\n      testId,\n      text,\n      valueText,\n    ],\n  );\n  useSemanticNode(semantics);\n  const Root = slots?.root ?? InkText;\n  const customization = resolveComponentProps(\n    "Text",\n    { variant, size, unstyled, className },\n    {},\n    theme,\n  );\n  return (\n    <Root\n      {...(customization as TuilTextProps)}\n      {...props}\n      {...resolveSlotProps(slotProps?.root, {}, theme)}\n    >\n      {children}\n    </Root>\n  );\n}\n',
      },
    ],
    registryDependencies: [],
  },
] as const satisfies readonly RegistryItem[];
