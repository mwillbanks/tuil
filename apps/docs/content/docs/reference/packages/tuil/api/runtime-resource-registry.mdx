---
title: "RuntimeResourceRegistry"
description: "class exported by @mwillbanks/tuil."
icon: BracketsCurly
---

{/* Generated by tooling/docs/generate-reference.ts. */}

## class

Public class exported by `@mwillbanks/tuil`.



```ts
export class RuntimeResourceRegistry<TValue> {
  readonly #prefix: string;
  readonly #resources = new Map<string, TValue>();
  readonly #observers = new Set<() => void>();
  #sequence = 0;

  constructor(prefix: string) {
    this.#prefix = prefix;
  }

  register(value: TValue): RuntimeResourceEntry<TValue> {
    const entry = Object.freeze({
      id: `${this.#prefix}-${++this.#sequence}`,
      value,
    });
    this.#resources.set(entry.id, value);
    this.#notify();
    return entry;
  }

  unregister(id: string): void {
    if (!this.#resources.delete(id)) return;
    this.#notify();
  }

  unregisterValue(value: TValue): void {
    for (const [id, candidate] of this.#resources) {
      if (candidate !== value) continue;
      this.unregister(id);
      return;
    }
  }

  entries(): readonly RuntimeResourceEntry<TValue>[] {
    return Object.freeze(
      [...this.#resources].map(([id, value]) => Object.freeze({ id, value })),
    );
  }

  values(): readonly TValue[] {
    return Object.freeze([...this.#resources.values()]);
  }

  observe(observer: () => void): () => void {
    this.#observers.add(observer);
    return () => this.#observers.delete(observer);
  }

  clear(): void {
    if (this.#resources.size === 0) return;
    this.#resources.clear();
    this.#notify();
  }

  #notify(): void {
    for (const observer of this.#observers) {
      try {
        observer();
      } catch {
        // Resource observers are diagnostic consumers and must not unwind a
        // completed registry mutation or prevent other observers from running.
      }
    }
  }
}
```

## Members

| Member | Type | Required | Description | Related types |
| --- | --- | --- | --- | --- |
| `#prefix` | `string` | Yes | The `#prefix` member uses the `string` contract. | — |
| `#resources` | `Map<string, TValue>` | Yes | The `#resources` member uses the `Map<string, TValue>` contract. | — |
| `#observers` | `Set<() => void>` | Yes | The `#observers` member uses the `Set<() => void>` contract. | — |
| `#sequence` | `number` | Yes | The `#sequence` member uses the `number` contract. | — |
| `__constructor` | `any` | Yes | The `__constructor` member uses the `any` contract. | — |
| `register` | `(value: TValue) => RuntimeResourceEntry<TValue>` | Yes | The `register` member uses the `(value: TValue) => RuntimeResourceEntry<TValue>` contract. | [`RuntimeResourceEntry`](/docs/reference/packages/tuil/api/runtime-resource-entry) |
| `unregister` | `(id: string) => void` | Yes | The `unregister` member uses the `(id: string) => void` contract. | — |
| `unregisterValue` | `(value: TValue) => void` | Yes | The `unregisterValue` member uses the `(value: TValue) => void` contract. | — |
| `entries` | `() => readonly RuntimeResourceEntry<TValue>[]` | Yes | The `entries` member uses the `() => readonly RuntimeResourceEntry<TValue>[]` contract. | [`RuntimeResourceEntry`](/docs/reference/packages/tuil/api/runtime-resource-entry) |
| `values` | `() => readonly TValue[]` | Yes | The `values` member uses the `() => readonly TValue[]` contract. | — |
| `observe` | `(observer: () => void) => () => void` | Yes | The `observe` member uses the `(observer: () => void) => () => void` contract. | — |
| `clear` | `() => void` | Yes | The `clear` member uses the `() => void` contract. | — |
| `#notify` | `() => void` | Yes | The `#notify` member uses the `() => void` contract. | — |

## Parameters

This declaration has no public members.

## Returns

This declaration does not return a value.

## Throws

No thrown errors are documented for this declaration.

## Related types

- [`RuntimeResourceEntry`](/docs/reference/packages/tuil/api/runtime-resource-entry)

## Source

[View the secondary source reference](https://github.com/mwillbanks/tuil/blob/main/packages/tuil/src/index.ts#L352)

## Package

[@mwillbanks/tuil](/docs/reference/packages/tuil)
