---
title: "RuntimeRenderTelemetry"
description: "class exported by @mwillbanks/tuil."
icon: BracketsCurly
---

{/* Generated by tooling/docs/generate-reference.ts. */}

## class

Public class exported by `@mwillbanks/tuil`.



```ts
export class RuntimeRenderTelemetry {
  #snapshot: RuntimeRenderSnapshot = Object.freeze({
    paused: false,
    sequence: 0,
  });

  snapshot(): RuntimeRenderSnapshot {
    return this.#snapshot;
  }

  record(
    value: Omit<RuntimeRenderSnapshot, "paused" | "sequence">,
  ): RuntimeRenderSnapshot {
    this.#snapshot = Object.freeze({
      ...value,
      paused: this.#snapshot.paused,
      sequence: this.#snapshot.sequence + 1,
    });
    return this.#snapshot;
  }

  pause(): void {
    this.#snapshot = Object.freeze({ ...this.#snapshot, paused: true });
  }

  resume(): void {
    this.#snapshot = Object.freeze({ ...this.#snapshot, paused: false });
  }
}
```

## Members

| Member | Type | Required | Description | Related types |
| --- | --- | --- | --- | --- |
| `#snapshot` | `RuntimeRenderSnapshot` | Yes | The `#snapshot` member uses the `RuntimeRenderSnapshot` contract. | [`RuntimeRenderSnapshot`](/docs/reference/packages/tuil/api/runtime-render-snapshot) |
| `snapshot` | `() => RuntimeRenderSnapshot` | Yes | The `snapshot` member uses the `() => RuntimeRenderSnapshot` contract. | [`RuntimeRenderSnapshot`](/docs/reference/packages/tuil/api/runtime-render-snapshot) |
| `record` | `(value: Omit<RuntimeRenderSnapshot, "paused" \| "sequence">) => RuntimeRenderSnapshot` | Yes | The `record` member uses the `(value: Omit<RuntimeRenderSnapshot, "paused" \| "sequence">) => RuntimeRenderSnapshot` contract. | [`RuntimeRenderSnapshot`](/docs/reference/packages/tuil/api/runtime-render-snapshot) |
| `pause` | `() => void` | Yes | The `pause` member uses the `() => void` contract. | — |
| `resume` | `() => void` | Yes | The `resume` member uses the `() => void` contract. | — |

## Parameters

This declaration has no public members.

## Returns

This declaration does not return a value.

## Throws

No thrown errors are documented for this declaration.

## Related types

- [`RuntimeRenderSnapshot`](/docs/reference/packages/tuil/api/runtime-render-snapshot)

## Source

[View the secondary source reference](https://github.com/mwillbanks/tuil/blob/main/packages/tuil/src/index.ts#L317)

## Package

[@mwillbanks/tuil](/docs/reference/packages/tuil)
